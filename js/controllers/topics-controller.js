// Generated by CoffeeScript 1.6.2
(function() {
  var Controller, Repo, Topic, TopicPageView, TopicsController, User, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Controller = require('controllers/base/controller');

  Topic = require('models/topic');

  TopicPageView = require('views/topic/topic-page-view');

  User = require('models/user');

  Repo = require('models/repo');

  module.exports = TopicsController = (function(_super) {
    __extends(TopicsController, _super);

    function TopicsController() {
      _ref = TopicsController.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    TopicsController.prototype.index = function(params) {
      return this.redirectToName('repos#show', params.login, params.repoName);
    };

    TopicsController.prototype.show = function(params) {
      this.user = new User({
        login: params.login
      });
      this.repo = new Repo({
        user: this.user,
        name: params.repoName
      });
      this.model = new Topic({
        repo: this.repo,
        number: params.topicNumber
      });
      this.view = new TopicPageView({
        model: this.model
      });
      return this.model.fetch();
    };

    return TopicsController;

  })(Controller);

}).call(this);
